paths:
  data_dir: data
  templates_dir: vorlagen
  image_dir: img
  hours_dir: stunden
  backup_dir: backup

pdf:
  engine: weasyprint

runtime:
  locale: de_DE.UTF-8

logging:
  enabled: true
  directory: logs
  level: INFO
